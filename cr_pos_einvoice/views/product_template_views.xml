<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_form_inherit_cr_other_charge_line" model="ir.ui.view">
        <field name="name">product.template.form.inherit.cr.other.charge.line</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <!--
            Anchor on a stable field (`available_in_pos`) instead of the tab/page name,
            which changed across Odoo versions and caused upgrade ParseError.
            -->
            <xpath expr="//field[@name='available_in_pos']" position="after">
                <field name="cr_is_other_charge_line"/>
            </xpath>
        </field>
    </record>
</odoo>
