<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pos_config_form_inherit_einvoice_cr" model="ir.ui.view">
        <field name="name">pos.config.form.einvoice.cr</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/div[hasclass('o_settings_container')][2]" position="inside">
                <setting id="pos_einvoice_cr_enable" string="Factura electrónica Costa Rica en POS">
                    <field name="l10n_cr_enable_einvoice_from_pos"/>
                    <div class="text-muted">
                        Si en el cobro marca "Facturar", se enviará Factura Electrónica; si no, se enviará Tiquete Electrónico.
                        El método y condición FE se toman de cada método de pago del POS.
                    </div>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
